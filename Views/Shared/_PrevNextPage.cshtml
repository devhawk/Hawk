@model Tuple<string, string>

<div class="prev-next-page">
    @if (!string.IsNullOrWhiteSpace(Model.Item1))
    {
        <a class="btn btn-primary" href="@Model.Item1">← Older Posts</a>
    }

    @if (!string.IsNullOrWhiteSpace(Model.Item2))
    {
        <a class="btn btn-primary" href="@Model.Item2">Newer Posts →</a>
    }
</div>
